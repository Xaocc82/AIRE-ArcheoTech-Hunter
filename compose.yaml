services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: archeotech
      POSTGRES_USER: archeotech
      POSTGRES_PASSWORD: archeotech
    ports:
      - "5432:5432"
    volumes:
      - archeotech-postgres:/var/lib/postgresql/data

volumes:
  archeotech-postgres:
